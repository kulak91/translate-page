import { PageTemplateId } from "../../enums/enums";
import { PageMeta } from "../common/common";
import { ServicesSection } from "../sections/sections";

type DevelopersPageContent = {
  id: number;
  __component: PageTemplateId.DEVELOPERS_PAGE;
  meta: PageMeta;
};

export { DevelopersPageContent };
